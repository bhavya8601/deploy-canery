apiVersion: v1
kind: Service
metadata:
  name: my-website-service
  namespace: canary11
spec:
  selector:
    app: my-website
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30088
  type: NodePort
